## Frontend Environment

Create a `.env` file in `frontend/` to configure the API base URL (optional):

```
VITE_API_BASE_URL=http://localhost:5000
```

Start the frontend:

```
npm install
npm run dev
```
*** End Patch

